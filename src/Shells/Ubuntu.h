/** For everything else */
// #ifdef __unix__ || __APPLE__ || __MACH__ || __linux__

#include "../Models/Shell.h"
/*
#pragma region 1. cd.
void Shell::Change_Directory(string path) {
    cout << "Unix Changed directory to " << path << endl;
}

#pragma endregion
// #endif
*/